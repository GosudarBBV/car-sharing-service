# src/test/resources/application-test.properties

# Database - ?????????????? Testcontainers
spring.datasource.url=jdbc:tc:mysql:8.0:///testdb
spring.datasource.username=test
spring.datasource.password=test
spring.datasource.driver-class-name=org.testcontainers.jdbc.ContainerDatabaseDriver

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Liquibase (???????? ??? ??????)
spring.liquibase.enabled=false

# Server
server.servlet.context-path=/api

# JWT - ?????????????? ??????? 256 ??? (32 ?????)
jwt.secret=test_secret_key_32_bytes_long_for_hmac_sha_256
jwt.expiration=3600000

# Stripe - dummy
stripe.secret.key=${STRIPE_SECRET_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}

# Telegram - ????????
telegram.bot.enabled=false
telegram.bot.token=dummy_token
telegram.bot.username=dummy_bot
telegram.admin.chat-id=0
telegram.bot.register-on-startup=false
